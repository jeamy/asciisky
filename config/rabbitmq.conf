# RabbitMQ 4.1 Configuration for ASCII Sky
# ==========================================

# Management Plugin Configuration
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

# AMQP Configuration
listeners.tcp.default = 5672

# Default User (overridden by environment variables)
# default_user = admin
# default_pass = password
# default_vhost = /

# Performance Tuning

# Logging
log.console = true
log.console.level = warning
log.file = false

# Memory and Disk Thresholds
vm_memory_high_watermark.relative = 0.6
disk_free_limit.absolute = 2GB

# Connection Limits
channel_max = 2048

# RabbitMQ 4.1 Performance Optimizations
# Quorum Queues are default and 2x faster than 3.x
queue_master_locator = min-masters

# Connection Tuning for 4.1
heartbeat = 60
consumer_timeout = 1800000  # 30 minutes
